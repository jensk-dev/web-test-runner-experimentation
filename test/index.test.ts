import { expect } from "@esm-bundle/chai";

describe('myFunction', () => {
  it('is always true', () => {
    expect(true).to.equal(true);
  });

  it('is always false', () => {
    expect(true).to.equal(false);
  });
});
